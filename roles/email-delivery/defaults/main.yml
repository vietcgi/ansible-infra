---
# Email Delivery Platform - Complete Configuration

# Domain & Infrastructure
email_domain: "vietcgi.nguoivietcali.com"
email_server_ip: "108.181.38.69"
email_hostname: "{{ email_domain }}"

# Cloudflare DNS Configuration
cloudflare_api_token: "{{ vault_cloudflare_api_token }}"  # Set via ansible-vault
cloudflare_zone_id: "{{ vault_cloudflare_zone_id }}"      # Set via ansible-vault
cloudflare_email_records:
  - name: "@"
    type: "A"
    content: "{{ email_server_ip }}"
    ttl: 3600
    proxied: false

# DKIM Configuration
dkim_selector: "default"
dkim_key_size: 2048

# DMARC Configuration
dmarc_policy: "quarantine"  # none, quarantine, reject
dmarc_subdomain_policy: "quarantine"
dmarc_report_email: "postmaster@{{ email_domain }}"
dmarc_forensics_email: "security@{{ email_domain }}"
dmarc_percent: 100

# SPF Configuration
spf_include_domains: []  # Additional domains to include in SPF

# Let's Encrypt TLS Certificates
letsencrypt_email: "admin@{{ email_domain }}"
letsencrypt_domains:
  - "{{ email_domain }}"
  - "mail.{{ email_domain }}"

# KumoMTA Integration
kumomta_enabled: true
kumomta_deliver_enabled: true
kumomta_domain: "{{ email_domain }}"
kumomta_hostname: "{{ email_hostname }}"
kumomta_dkim_enabled: true
kumomta_dkim_selector: "{{ dkim_selector }}"
kumomta_max_delivery_attempts: 5
kumomta_max_retry_delay: 86400
kumomta_max_messages_per_minute: 300
kumomta_max_message_size: 104857600  # 100MB
kumomta_sender_messages_per_minute: 100

# Email Routing
email_relay_enabled: false
email_alternate_domains: []  # Additional domains for local delivery
kumomta_relay_enabled: false
kumomta_relay_host: ""  # Set to enable relay delivery
kumomta_relay_port: 25
kumomta_relay_use_tls: false
kumomta_relay_require_tls: false
email_allowed_senders: []
email_rejected_domains: []

# Monitoring
monitoring_enabled: true
grafana_dashboard_enabled: true
prometheus_alerts_enabled: true

# Backup & Recovery
backup_enabled: true
backup_retention_days: 30

---
# Cloudflare DNS Configuration
# Note: DNS records are configured via the main playbook's cloudflare_integration role
# This file is only included if cloudflare_api_token is provided to the email-delivery role

- name: Display DNS Configuration Skipped
  ansible.builtin.debug:
    msg: |
      ⊘ DNS configuration skipped
      Note: DNS records are configured via the main playbook's cloudflare_integration role
      Email-delivery role does not duplicate DNS configuration
